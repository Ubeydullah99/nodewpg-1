let firstNumber = 23;
let secondNumber = 57;
if (firstNumber === secondNumber) {
    console.log('They are the smae number') ;
} else if (firstNumber > secondNumber) {
    console.log(`${firstNumber} is bogger than ${secondNumber}`);
} else {
    console.log(`${secondNumber} is bigger than ${firstNumber}`);
}